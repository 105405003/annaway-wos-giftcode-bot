# 🚀 WOS 機器人部署快速參考

**完整指南：** 請參閱 `GCP_部署完整指南.md`

---

## 📋 部署檢查清單

### 前置準備
- [ ] Discord Bot Token（從 Discord Developer Portal 取得）
- [ ] Discord 伺服器已創建 `Annaway_Admin` 和 `Annaway_Manager` 角色
- [ ] GCP 帳號和專案
- [ ] 本地程式碼已更新到最新版

---

## ⚡ 快速指令集

### 1. 本地打包（Windows PowerShell）

```powershell
cd F:\AnnawayProjects\wos_giftcode_redemption_bot

$timestamp = Get-Date -Format 'yyyyMMdd_HHmmss'
$zipName = "wos_bot_deploy_$timestamp.zip"

# 簡化版打包（只打包必要檔案）
Compress-Archive -Path @(
    "main.py",
    "cogs",
    "utils",
    "i18n",
    "models",
    "fonts",
    "permission_manager.py",
    "i18n_manager.py",
    "i18n_config.py",
    "requirements.txt",
    "README.md",
    "BOT_PERMISSIONS_GUIDE.md",
    "bot_config.env.example",
    "cleanup_gcp_vm.sh"
) -DestinationPath $zipName -Force

Write-Host "✅ 打包完成: $zipName" -ForegroundColor Green
```

### 2. 首次部署（GCP VM）

```bash
# === 系統準備 ===
sudo apt update
sudo apt install -y python3 python3-pip python3-venv unzip

# === 創建專案 ===
mkdir -p ~/wos_bot
cd ~/wos_bot

# === 解壓縮 ===
unzip -o ~/wos_bot_deploy_*.zip -d ~/wos_bot

# === 設定虛擬環境 ===
python3 -m venv ~/wos_bot/bot_venv
source ~/wos_bot/bot_venv/bin/activate
pip install --upgrade pip
pip install -r ~/wos_bot/requirements.txt

# === 創建資料庫目錄 ===
mkdir -p ~/wos_bot/db

# === 設定 Token ===
cp ~/wos_bot/bot_config.env.example ~/wos_bot/bot_config.env
nano ~/wos_bot/bot_config.env
# 填入：BOT_TOKEN=你的Token

# === 設定 systemd 服務 ===
sudo nano /etc/systemd/system/wos-bot.service
```

**服務檔內容：**
```ini
[Unit]
Description=WOS Gift Code Bot
After=network.target

[Service]
Type=simple
User=你的使用者名稱
WorkingDirectory=/home/你的使用者名稱/wos_bot
Environment="PATH=/home/你的使用者名稱/wos_bot/bot_venv/bin"
ExecStart=/home/你的使用者名稱/wos_bot/bot_venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

```bash
# === 啟用並啟動 ===
sudo systemctl daemon-reload
sudo systemctl enable wos-bot
sudo systemctl start wos-bot
sudo systemctl status wos-bot
```

### 3. 更新部署（已有環境）

```bash
# 停止服務
sudo systemctl stop wos-bot

# 備份資料庫（重要！）
cp -r ~/wos_bot/db/ ~/wos_bot_backup_$(date +%Y%m%d_%H%M%S)/

# 解壓縮新版本
cd ~/wos_bot
unzip -o ~/wos_bot_deploy_*.zip

# 清理快取
find ~/wos_bot -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null
find ~/wos_bot -type f -name "*.pyc" -delete

# 啟動服務
sudo systemctl start wos-bot

# 查看狀態
sudo systemctl status wos-bot
sudo journalctl -u wos-bot -f
```

---

## 🔍 常用除錯指令

```bash
# 查看服務狀態
sudo systemctl status wos-bot

# 查看即時日誌
sudo journalctl -u wos-bot -f

# 查看最近 50 行日誌
sudo journalctl -u wos-bot -n 50

# 查看權限除錯
tail -50 ~/wos_bot/permission_debug.log

# 手動執行（除錯用）
cd ~/wos_bot
source bot_venv/bin/activate
python main.py
```

---

## ⚠️ 常見問題快速修復

### 問題：機器人無法啟動

```bash
# 檢查日誌找原因
sudo journalctl -u wos-bot -n 100 --no-pager

# 常見原因 1：Token 錯誤
nano ~/wos_bot/bot_config.env

# 常見原因 2：缺少套件
source ~/wos_bot/bot_venv/bin/activate
pip install -r ~/wos_bot/requirements.txt

# 常見原因 3：權限問題
sudo chown -R $USER:$USER ~/wos_bot
```

### 問題：「機器人需要管理伺服器權限」

```bash
# 這是舊版本的 bug，確認已更新
grep -A 2 "機器人不需要 manage_guild" ~/wos_bot/cogs/alliance.py

# 如果沒有這行註解，需要重新部署最新版本
# 然後重啟
sudo systemctl restart wos-bot
```

### 問題：斜線命令不顯示

```bash
# 1. 確認機器人在線
sudo systemctl status wos-bot

# 2. 等待 Discord 同步（5-10 分鐘）

# 3. 檢查日誌
sudo journalctl -u wos-bot | grep "Commands synced"

# 4. 重啟 Discord 應用程式
```

### 問題：權限檢查失敗

```bash
# 查看權限日誌
tail -50 ~/wos_bot/permission_debug.log

# 確認角色名稱（必須完全一致）
# - Annaway_Admin
# - Annaway_Manager
```

---

## 🧹 維護指令

```bash
# 清理舊的部署 ZIP（保留最近 7 天）
find ~ -maxdepth 1 -name "wos_bot_deploy_*.zip" -mtime +7 -delete

# 清理舊備份（保留最新 3 個）
ls -t ~/wos_bot_backup_* | tail -n +4 | xargs rm -rf

# 查看磁碟使用
du -sh ~/wos_bot
du -sh ~/wos_bot/db/*

# 備份資料庫
cp -r ~/wos_bot/db/ ~/wos_bot_backup_manual_$(date +%Y%m%d)/
```

---

## 📞 Discord Developer Portal 設定重點

### 必須啟用的 Intents：
- ✅ Server Members Intent
- ✅ Message Content Intent

### 機器人需要的權限（權限值：277025508416）：
- ✅ View Channels
- ✅ Send Messages
- ✅ Embed Links
- ✅ Attach Files
- ✅ Read Message History
- ✅ Use Application Commands

### 不需要的權限：
- ❌ Administrator
- ❌ Manage Server
- ❌ Manage Channels
- ❌ Manage Roles

---

## 🎯 部署驗證

部署完成後，按順序測試：

1. **檢查服務**
   ```bash
   sudo systemctl status wos-bot
   # 應該顯示：Active: active (running)
   ```

2. **檢查 Discord**
   - 機器人顯示為線上（綠色圓點）

3. **測試指令**
   - 在 Discord 輸入：`/settings`
   - 應該看到選單

4. **測試權限**
   - 點擊「成員操作」按鈕
   - 應該立即回應，沒有錯誤

5. **檢查日誌**
   ```bash
   tail -20 ~/wos_bot/permission_debug.log
   # 應該看到：✅ ALLOWED
   ```

---

## 📱 服務管理速查

| 操作 | 指令 |
|------|------|
| 啟動 | `sudo systemctl start wos-bot` |
| 停止 | `sudo systemctl stop wos-bot` |
| 重啟 | `sudo systemctl restart wos-bot` |
| 狀態 | `sudo systemctl status wos-bot` |
| 即時日誌 | `sudo journalctl -u wos-bot -f` |
| 歷史日誌 | `sudo journalctl -u wos-bot -n 100` |
| 啟用自動啟動 | `sudo systemctl enable wos-bot` |
| 停用自動啟動 | `sudo systemctl disable wos-bot` |

---

## 🔐 安全注意事項

1. **永遠不要分享：**
   - Discord Bot Token
   - `bot_config.env` 檔案內容
   - GCP SSH 金鑰

2. **定期備份：**
   ```bash
   # 每週備份一次
   cp -r ~/wos_bot/db/ ~/wos_bot_backup_$(date +%Y%m%d)/
   ```

3. **權限最小化：**
   - 機器人只需要基本權限
   - 不要給予管理員權限

---

## 📚 完整文件

- **`GCP_部署完整指南.md`** - 詳細部署步驟（推薦新手閱讀）
- **`BOT_PERMISSIONS_GUIDE.md`** - Discord 權限詳細說明
- **`README_DEPLOY_TW.md`** - 繁體中文部署說明
- **`QUICK_START.md`** - 快速開始指南

---

**最後更新：** 2026-01-25  
**維護者：** Annaway Studio
